{
  "name": "portifolio",
  "private": true,
  "scripts": {
    "build:images": "node scripts/build-images.mjs",
    "build:minify": "node scripts/minify.mjs",
    "build": "npm run build:images && npm run build:minify",
    "serve": "npx serve -s -l 5501 --compress ."
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "lightningcss": "^1.25.1",
    "sharp": "^0.33.5"
  }
}
